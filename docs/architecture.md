# System Architecture

ProductPulse consists of:

# Data Layer (Airtable)
stores reports
runs scoring formulas

# Logic Layer
severity scoring
trust weighting
risk calculation

# Presentation Layer
dashboard interface
analytics charts
A form to add a product
